info:
  name: Slots
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/slots
  body:
    type: multipart-form
    data:
      - name: pbo
        type: file
        value:
          - /home/agentos/Downloads/VTG_Suchasna_Viyna_143_v01.gc_krayina.pbo
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
